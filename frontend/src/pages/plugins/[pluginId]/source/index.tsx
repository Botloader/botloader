import { RouteObject } from "react-router-dom";
import { ViewPluginSource } from "../ViewPlugin";

export const routes: RouteObject[] = [
    {
        index: true,
        element: <ViewPluginSource />,
    }
]
